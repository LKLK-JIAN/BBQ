<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.benetech.bbq">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main3Activity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".BluetoothActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChannelActivity"
            android:launchMode="singleTask" />
        <activity android:name=".Main4Activity" />
        <activity android:name=".TempReminderActivity" />
        <activity android:name=".ReminderActivity" />
        <activity android:name=".channel.ChanneloneActivity" />
        <activity android:name=".channel.ChanneltwoActivity" />
        <activity android:name=".FileActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".timedown.Main2Activity" />
        <activity android:name=".wight.TimeDownActivity" />
        <service android:name=".service.TimedownService" />
        <service android:name=".service.Timedown2Service" />
        <service android:name=".service.Timedown3Service" />
        <service android:name=".service.Timedown4Service" />
        <activity android:name=".DocumentActivity" />
        <activity android:name=".ContentActivity"></activity>

    </application>

</manifest>